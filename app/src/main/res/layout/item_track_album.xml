<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout2"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/prev_play"
        android:layout_width="34dp"
        android:layout_height="33dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:adjustViewBounds="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@mipmap/play" />

    <TextView
        android:id="@+id/prev_songname"
        android:layout_width="185dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_toEndOf="@+id/prev_play"
        android:layout_toRightOf="@+id/prev_play"
        android:textColor="@color/white"
        app:layout_constraintStart_toEndOf="@+id/prev_play"
        app:layout_constraintTop_toTopOf="@+id/prev_play" />

    <ImageButton
        android:id="@+id/live_song_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:background="@color/transparent"
        android:src="@mipmap/live_song_btn"
        app:layout_constraintBottom_toBottomOf="@+id/album_track_btn"
        app:layout_constraintEnd_toStartOf="@+id/album_track_btn"
        app:layout_constraintTop_toTopOf="@+id/album_track_btn"
        app:layout_constraintVertical_bias="0.0"
        tools:src="@mipmap/live_song_btn" />

    <ImageButton
        android:id="@+id/album_track_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:background="@color/transparent"
        android:src="@mipmap/song_post_btn"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@mipmap/song_post_btn" />
</androidx.constraintlayout.widget.ConstraintLayout>